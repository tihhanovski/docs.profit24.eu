# Сопутствующие товары

Некоторые товары могут **притягивать за собой дополнительные товары**: когда основной товар используется в строке документа, эти дополнительные товары добавляются автоматически. Типичные примеры – комплекты, залоги (депозиты), части набора или другие товары, которые необходимо поставлять вместе с основным.

!!! note "Включение функции"
    Функция является глобальной настройкой, которую можно включить или отключить в настройках компании. Если она отключена, строки автоматически не добавляются.

## Где работает функция

Сопутствующие товары работают в документах продажи, покупки и склада:

- счета продажи;
- коммерческие предложения;
- заказы продажи;
- счета покупки;
- накладные и приходы на склад.

## Как это выглядит для пользователя

1. Выберите товар в строке документа (или измените уже введённый).
2. Если у этого товара заданы сопутствующие товары, программа добавляет соответствующие строки **после** основной строки.
3. **Количество** добавленной строки рассчитывается из количества основной строки: **количество основной строки × количество сопутствующего товара**.
4. Автоматически добавленные строки – обычные строки, при необходимости их можно изменить (например, цену, стоимость).

## Изменения во время составления документа

- При изменении количества основной строки количество сопутствующих строк обновляется соответственно.
- Введённые вручную значения (например, стоимость или цена) при этом сохраняются.
- Если вы сами изменили **количество** добавленной строки, программа больше не перезаписывает его автоматически.

## Удаление основной строки

При удалении основной строки сопутствующие строки **не удаляются** – они становятся самостоятельными обычными строками, которые можно отдельно изменить или удалить.

## Задание сопутствующих товаров

Сопутствующие товары задаются среди связей (ссылок) товара, используя соответствующий тип связи (сопутствующий товар).

!!! note "Срок действия связи"
    Связь между основной строкой и добавленными через неё строками действует во время **составления документа**. При сохранении документа добавленные строки становятся обычными строками.

!!! info
    Модуль находится в активной разработке, и в него постоянно добавляются новые возможности. Если вы не нашли ответ в руководстве, свяжитесь с нами по адресу [info@intellisoft.ee](mailto:info@intellisoft.ee).
